name = "studioh-scrape"
main = "src/index.js"
compatibility_date = "2026-08-01"

# No secrets, no bindings — this worker only fetches public pages and returns
# structured data. Keep it separate from studioh-ai so a change here can never
# break the plant book, projects or image storage.

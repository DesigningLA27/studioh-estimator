const{chromium}=require('/Users/warwick/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage({viewport:{width:1512,height:1000}});let errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:8769/deployment/studioh-estimator-gpt/v2/');await p.waitForFunction(()=>v2Preview.ready);const f=p.frames()[1];assert(await p.getByRole('heading',{name:'Your project journey'}).isVisible());assert(await p.getByRole('heading',{name:'Next steps',exact:true}).isVisible());assert(await p.getByRole('heading',{name:'Latest activity'}).count());await p.locator('[data-experience=customer]').click();assert(!(await p.locator('#v2-rail [data-page=financials]').isVisible()));await p.evaluate(()=>v2Preview.show('algos','Algorithms'));assert(!(await p.locator('#v2-engine-pane').isVisible()));await p.evaluate(()=>v2Workspace.open('insights'));await f.waitForSelector('#v2-insights');assert.equal(await f.evaluate(()=>v2InsightRole),'customer');assert.equal(await f.locator('[data-role]').count(),0);await f.locator('[data-category=all]').click();assert.equal(await f.locator('.vi-card').count(),await f.evaluate(()=>INS_CARDS.filter(c=>c.client===1).length));await p.locator('[data-experience=developer]').click();await p.evaluate(()=>v2Workspace.open('insights'));await f.waitForSelector('#v2-insights');await f.locator('[data-category=all]').click();assert.equal(await f.locator('.vi-card').count(),await f.evaluate(()=>INS_CARDS.length));await f.locator('[data-customize]').click();await f.locator('[data-select=total]').uncheck();await f.locator('[data-done]').click();await p.evaluate(()=>v2Preview.show('photos','Photos & references'));await f.waitForSelector('#v2-photos');await f.evaluate(()=>{const c=document.createElement('canvas');c.width=800;c.height=600;c.getContext('2d').fillRect(0,0,800,600);for(let i=0;i<6;i++)v2PhotoItems().push({id:'test'+i,name:'Photo '+i,url:c.toDataURL('image/png'),kind:'Site overview'});v2PhotoDraw()});const width1=await f.locator('.v2-photo-card').first().evaluate(e=>e.clientWidth);await f.locator('#v2-photo-size').fill('6');const width2=await f.locator('.v2-photo-card').first().evaluate(e=>e.clientWidth);assert(width2>width1);assert.equal(await f.locator('.v2-photo-card img').first().evaluate(e=>getComputedStyle(e).objectFit),'cover');await f.locator('[data-favorite="0"]').click();assert(await f.evaluate(()=>S.designBrief.photos[0].favorite));await f.locator('[data-photo="0"]').click();await f.locator('[name=tags]').fill('slope, shade');await f.locator('[data-save]').click();await f.locator('#v2-photo-search').fill('shade');assert.equal(await f.locator('.v2-photo-card').count(),1);await p.waitForTimeout(1100);await p.reload();await p.waitForFunction(()=>v2Preview.ready);const ff=p.frames()[1];assert(await p.getByRole('heading',{name:'Your project journey'}).isVisible());assert.equal(await ff.evaluate(()=>S.designBrief.photos[0].favorite),true);assert.deepEqual(await ff.evaluate(()=>S.designBrief.photos[0].tags),['slope','shade']);assert(!(await ff.evaluate(()=>S.v2Dashboards.developer.insights.includes('total'))));await p.evaluate(()=>v2Workspace.open('brief'));for(const theme of ['Dusk','Night']){await p.locator('[data-theme='+theme+']').click();await p.screenshot({path:'/tmp/brief23-'+theme+'.png'})}await p.evaluate(()=>v2Preview.show('clientbrief','Questionnaire'));await p.waitForTimeout(1600);let q=p.frames().find(x=>x!==p.mainFrame()&&x!==ff);console.log('questionnaire nav',await q.locator('.shell nav button').count());let report=[];for(let i=0;i<9;i++){await q.locator('.shell nav button').nth(i).click();await p.waitForTimeout(50);const bright=await q.evaluate(()=>[...document.querySelectorAll('.body *')].filter(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e),v=s.backgroundColor.match(/[\d.]+/g)||[];return r.width>80&&r.height>35&&v.length>=3&&v.slice(0,3).every(x=>+x>200)&&(+v[3]||1)>.9}).map(e=>({tag:e.tagName,cls:e.className,color:getComputedStyle(e).backgroundColor})));report.push({i,bright})}console.log(JSON.stringify(report));for(const width of [834,390]){await p.setViewportSize({width,height:1000});await p.evaluate(()=>v2Workspace.open('home'));assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.screenshot({path:'/tmp/home23-'+width+'.png'})}assert.equal(errors.length,0,errors.join('\n'));console.log('PASS: Home restored; global modes; all insight cards; slider, cover, favorites, tags, persistence; responsive');await p.goto('http://127.0.0.1:8769/deployment/studioh-estimator-gpt/v2/mockups/experience-review/');await p.screenshot({path:'/tmp/mock23.png'});}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});

const {chromium}=require('/Users/warwick/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{const p=await b.newPage({viewport:{width:1280,height:960}});const errors=[],calls=[];p.on('pageerror',e=>errors.push(e.message));p.on('request',r=>{if(r.method()==='POST'&&r.url().includes('workers.dev'))calls.push(r.postDataJSON())});await p.route('https://designingla27.github.io/studioh-estimator/v2/**',async r=>{let name=new URL(r.request().url()).pathname.split('/v2/')[1]||'index.html';const path=require('path').resolve(__dirname,'..',name);if(require('fs').existsSync(path))await r.fulfill({path});else await r.continue()});await p.goto('https://designingla27.github.io/studioh-estimator/v2/');await p.waitForFunction(()=>v2Preview.ready);await p.evaluate(()=>localStorage.setItem('studioh_favs_v1','V1 sentinel'));for(const id of ['plantbook','materials','furnishings','colorlibrary']){await p.evaluate(id=>v2Workspace.open(id),id);await p.waitForFunction(()=>document.querySelector('#catalog-count')?.textContent.match(/^[1-9]/),{},{timeout:45000});await p.waitForFunction(()=>!document.querySelector('[data-catalog-refresh]').disabled);console.log(id,await p.locator('#catalog-count').textContent());if(id==='plantbook'){await p.click('[data-catalog-filter="favorites"]');console.log('favorites',await p.locator('#catalog-count').textContent());assert(parseInt(await p.locator('#catalog-count').textContent())>0);await p.waitForFunction(()=>Array.from(document.querySelectorAll('.catalog-photo img')).some(i=>i.complete&&i.naturalWidth>0),{},{timeout:30000});await p.screenshot({path:'/tmp/v2-real-plants.png'});await p.click('[data-catalog-filter="favorites"]');await p.fill('#catalog-search','Quercus agrifolia');assert(await p.locator('.catalog-card').count()>0);await p.locator('[data-catalog-detail]').first().click();assert(await p.locator('#v2-catalog-detail').isVisible());await p.click('[data-catalog-close]');await p.locator('[data-catalog-favorite]').first().click();assert(await p.evaluate(()=>localStorage.getItem('studioh_v2_library_favorites_v1')));assert.equal(await p.locator('[data-catalog-tools]').count(),0);}
if(id==='furnishings'){await p.waitForFunction(()=>Array.from(document.querySelectorAll('.catalog-photo img')).some(i=>i.complete&&i.naturalWidth>0));await p.screenshot({path:'/tmp/v2-real-furnishings.png'})}
if(id==='colorlibrary'){assert(await p.locator('.catalog-palette span').count()>0);await p.screenshot({path:'/tmp/v2-real-colors.png'})}}
assert.equal(await p.evaluate(()=>localStorage.getItem('studioh_favs_v1')),'V1 sentinel');console.log('API operations',calls);assert(calls.every(x=>['loadbook','loadgoods','loadconfig'].includes(x.type)));assert(await p.frames()[1].evaluate(()=>PLANT_DB.tree.length>0));for(const w of [834,390]){await p.setViewportSize({width:w,height:960});assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));}await p.reload();await p.waitForFunction(()=>v2Preview.ready);await p.context().setOffline(true);await p.evaluate(()=>v2Workspace.open('plantbook'));await p.waitForFunction(()=>/^[1-9]/.test(document.querySelector('#catalog-count')?.textContent||''));console.log('offline cache ok');assert.equal(await p.evaluate(()=>localStorage.getItem('studioh_favs_v1')),'V1 sentinel');assert.deepEqual(errors,[]);console.log('PASS real records, photos, favorites, details, isolation, fixed read operations, responsive layout, offline cache');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});

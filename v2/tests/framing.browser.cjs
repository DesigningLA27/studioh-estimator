const {chromium}=require('/Users/warwick/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{const p=await b.newPage({viewport:{width:1280,height:960}});const errors=[],calls=[];p.on('pageerror',e=>errors.push(e.message));p.on('request',r=>{if(r.method()==='POST'&&r.url().includes('workers.dev'))calls.push(r.postDataJSON())});await p.route('https://designingla27.github.io/studioh-estimator/v2/**',async r=>{let name=new URL(r.request().url()).pathname.split('/v2/')[1]||'index.html';const path=require('path').resolve(__dirname,'..',name);if(require('fs').existsSync(path))await r.fulfill({path});else await r.continue()});await p.goto('https://designingla27.github.io/studioh-estimator/v2/');await p.waitForFunction(()=>v2Preview.ready);await p.evaluate(()=>localStorage.setItem('studioh_favs_v1','V1 sentinel'));await p.evaluate(()=>v2Workspace.open('plantbook'));await p.waitForFunction(()=>!document.querySelector('[data-catalog-refresh]').disabled);await p.fill('#catalog-search','Lagerstroemia indica');await p.click('[data-catalog-filter="photos"]');await p.waitForFunction(()=>[...document.querySelectorAll('.plant-photo img')].some(i=>i.complete&&i.naturalWidth>0));assert.equal(await p.locator('[data-frame-toggle]').count(),1);const metrics=await p.locator('.catalog-density').evaluate(e=>({height:e.getBoundingClientRect().height,text:e.textContent}));assert.equal(metrics.height,44);assert(!metrics.text.includes('Larger'));const img=p.locator('.plant-photo img').first();assert.equal(await img.evaluate(e=>getComputedStyle(e).objectFit),'cover');await p.click('[data-frame-toggle]');await p.waitForFunction(()=>document.querySelector('.plant-photo img')?.naturalWidth>0);await p.locator('[data-frame-zoom]').first().fill('2');const box=await p.locator('.plant-photo:has(img)').first().boundingBox();await p.mouse.move(box.x+box.width/2,box.y+box.height/2);await p.mouse.down();await p.mouse.move(box.x+box.width/2+35,box.y+box.height/2+20,{steps:6});await p.mouse.up();let frame=await p.evaluate(()=>JSON.parse(localStorage.getItem('studioh_v2_image_framing_v1')));assert(Object.values(frame).some(v=>v.z===2&&v.x!==50));await p.click('[data-frame-toggle]');assert.equal(await p.locator('[data-frame-zoom]').count(),0);assert.equal(await p.locator('.is-adjusting').count(),0);await p.screenshot({path:'/tmp/v2-framing-locked.png'});await p.reload();await p.waitForFunction(()=>v2Preview.ready);await p.evaluate(()=>v2Workspace.open('plantbook'));await p.waitForFunction(()=>!document.querySelector('[data-catalog-refresh]').disabled);await p.fill('#catalog-search','Lagerstroemia indica');await p.click('[data-catalog-filter="photos"]');assert.equal(await p.locator('[data-frame-zoom]').count(),0);assert.deepEqual(await p.evaluate(()=>JSON.parse(localStorage.getItem('studioh_v2_image_framing_v1'))),frame);await p.click('[data-frame-toggle]');await p.locator('[data-frame-reset]').first().click();frame=await p.evaluate(()=>JSON.parse(localStorage.getItem('studioh_v2_image_framing_v1')));assert(Object.values(frame).some(v=>v.z===1&&v.x===50&&v.y===50));assert.equal(await p.evaluate(()=>localStorage.getItem('studioh_favs_v1')),'V1 sentinel');assert(calls.every(x=>['loadbook','loadgoods','loadconfig'].includes(x.type)));assert.deepEqual(errors,[]);console.log('PASS automatic fill, 44px control, developer visibility, zoom, pan, lock, persistence, reset, V1 isolation');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
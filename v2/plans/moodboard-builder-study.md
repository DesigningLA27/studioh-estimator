# V2.034 — image-only board and builder study

Live moodboard: omit selections with missing image URLs from visual cards; retain project/specification membership. Explain omitted items with counts and expandable names. The wall has an aggregate missing-image count. Items reappear automatically once their library record has an image.

Compact support rail: palette summary, project count/budget summary, short design intention. Full palette editor opens in main content; full insights cards are in the main column.

Builder mockups (not a production builder): /v2/mockups/moodboard-builder/. Three interactive options: section planner beside preview, three-step guided setup, inline board construction. All simulate automatic section creation when adding a library item, section order, compact-card choices, and image omission. Changes stay only in mockup memory and never mutate a real project.

Recommended starting point: section planner, with the inline add-section strip for subsequent edits. User has not selected a builder yet. All five themes and phone widths verified. Login/server-storage work is separate.
